# Runtime dependencies for the Parsed-Visual RAG (v12-general) vs VisRAG
# benchmark harness in this workspace. Reproduces results/ppt_format/
# today_results_table.{md,csv,json} via scripts/run_today_ppt_pipeline.sh.
#
# NVIDIA driver 572.61 (CUDA 12.8 compatible). Install torch with CUDA 12.4
# wheels so an RTX 4060 (8 GB) is detected:
#   pip install --upgrade --force-reinstall torch torchvision \
#       --index-url https://download.pytorch.org/whl/cu124

# Core
datasets>=2.18
Pillow>=10.1
numpy>=1.26
rank_bm25>=0.2.2
fsspec<=2026.2.0
requests>=2.31

# Generator: Qwen2-VL-7B + bitsandbytes NF4 (workspace default)
torch>=2.6
torchvision>=0.21
transformers>=4.51,<4.52
qwen-vl-utils>=0.0.8
accelerate>=0.34
bitsandbytes>=0.43

# Optional generator backend: GPT-4o (paper API fallback)
openai>=1.30
python-dotenv>=1.0
