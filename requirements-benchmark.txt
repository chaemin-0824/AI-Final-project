# Lightweight dependencies for the comparison harness.
# Install full VisRAG dependencies separately when running model inference/GPU retrieval:
#   pip install -r visrag/requirements.txt && pip install -e visrag && (cd visrag/timm_modified && pip install -e .)

datasets>=2.18
Pillow>=10.1
numpy>=1.26
scipy>=1.13
openpyxl>=3.1
pandas>=2.2
google-generativeai>=0.7
python-dotenv>=1.0
langchain-core>=0.2
langchain-community>=0.2
rank_bm25>=0.2.2
PyMuPDF>=1.24

# Required by v12_port (paper_visual_rag-derived modules) at runtime
pdf2image>=1.17
scikit-learn>=1.5

# Generator backend deps (Qwen2-VL workspace default for PPT alignment).
# transformers 4.51.x is the latest stable line that exposes Qwen2VLForConditionalGeneration.
# Reinstall torch with cu124 wheels so RTX 4060 (driver 572.61 / cu128) sees GPU:
#   pip install --upgrade --force-reinstall torch torchvision \
#       --index-url https://download.pytorch.org/whl/cu124
transformers>=4.51,<4.52
qwen-vl-utils>=0.0.8
accelerate>=0.34
torch>=2.6
torchvision>=0.21
# 4-bit quantisation. We use bitsandbytes NF4 (replaces deprecated AutoAWQ).
bitsandbytes>=0.43
# datasets pin: keep fsspec under 2026.2.0 so datasets 4.x doesn't complain.
fsspec<=2026.2.0
